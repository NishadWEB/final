<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Profile - MediDiag</title>
    <link rel="stylesheet" href="/main/main.css">
    <link rel="stylesheet" href="/navbar/navbar.css">
    <link rel="stylesheet" href="/doctor/dashboard.css">
</head>
<body>
    <%- include('../headers/preloginheader') %>
    
    <div class="container doctor-dashboard">
        <div class="dashboard-grid">
            <aside class="sidebar">
                <nav class="sidebar-nav">
                    <ul>
                        <li><a href="/doctor/dashboard">Dashboard</a></li>
                        <li><a href="/doctor/appointments">Appointments</a></li>
                        <li><a href="/doctor/profile" class="active">Profile</a></li>
                        <li><a href="/auth/logout">Logout</a></li>
                    </ul>
                </nav>
            </aside>

            <main class="dashboard-content">
                <h1>Doctor Profile</h1>
                
                <div class="profile-section">
                    <div class="card">
                        <h2>Professional Information</h2>
                        <form id="profile-form">
                            <div class="form-row">
                                <div class="form-group">
                                    <label class="form-label">Full Name</label>
                                    <input type="text" name="full_name" class="form-control" value="<%= (doctor && doctor.full_name) || '' %>" required>
                                </div>
                                <div class="form-group">
                                    <label class="form-label">Email</label>
                                    <input type="email" class="form-control" value="<%= (doctor && doctor.email) || user.email || '' %>" disabled>
                                    <small>Contact support to change email</small>
                                </div>
                            </div>

                            <div class="form-row">
                                <div class="form-group">
                                    <label class="form-label">Specialization</label>
                                    <select name="specialization" class="form-control" required>
        <option value="">Select Specialization</option>

        <option value="Cardiology" <%= doctor && doctor.specialization === 'Cardiology' ? 'selected' : '' %>>Cardiology</option>
        <option value="Dermatology" <%= doctor && doctor.specialization === 'Dermatology' ? 'selected' : '' %>>Dermatology</option>
        <option value="Pediatrics" <%= doctor && doctor.specialization === 'Pediatrics' ? 'selected' : '' %>>Pediatrics</option>
        <option value="Neurology" <%= doctor && doctor.specialization === 'Neurology' ? 'selected' : '' %>>Neurology</option>
        <option value="Gynecology" <%= doctor && doctor.specialization === 'Gynecology' ? 'selected' : '' %>>Gynecology</option>
        <option value="Internal Medicine" <%= doctor && doctor.specialization === 'Internal Medicine' ? 'selected' : '' %>>Internal Medicine</option>
        <option value="Orthopedics" <%= doctor && doctor.specialization === 'Orthopedics' ? 'selected' : '' %>>Orthopedics</option>
        <option value="ENT" <%= doctor && doctor.specialization === 'ENT' ? 'selected' : '' %>>ENT</option>
        <option value="Psychiatry" <%= doctor && doctor.specialization === 'Psychiatry' ? 'selected' : '' %>>Psychiatry</option>
        <option value="Infectious Specialist" <%= doctor && doctor.specialization === 'Infectious Specialist' ? 'selected' : '' %>>Infectious Specialist</option>
        <option value="Dentistry" <%= doctor && doctor.specialization === 'Dentistry' ? 'selected' : '' %>>Dentistry</option>
        <option value="Hepatology" <%= doctor && doctor.specialization === 'Hepatology' ? 'selected' : '' %>>Hepatology</option>
        <option value="Ophthalmology" <%= doctor && doctor.specialization === 'Ophthalmology' ? 'selected' : '' %>>Ophthalmology</option>
        <option value="Endocrinology" <%= doctor && doctor.specialization === 'Endocrinology' ? 'selected' : '' %>>Endocrinology</option>
        <option value="Oncology" <%= doctor && doctor.specialization === 'Oncology' ? 'selected' : '' %>>Oncology</option>
        <option value="Pulmonology" <%= doctor && doctor.specialization === 'Pulmonology' ? 'selected' : '' %>>Pulmonology</option>
        <option value="Pathology" <%= doctor && doctor.specialization === 'Pathology' ? 'selected' : '' %>>Pathology</option>
        <option value="General Surgery" <%= doctor && doctor.specialization === 'General Surgery' ? 'selected' : '' %>>General Surgery</option>
        <option value="Urology" <%= doctor && doctor.specialization === 'Urology' ? 'selected' : '' %>>Urology</option>
        <option value="Neurosurgery" <%= doctor && doctor.specialization === 'Neurosurgery' ? 'selected' : '' %>>Neurosurgery</option>

    </select>
                                </div>
                                <div class="form-group">
                                    <label class="form-label">License Number</label>
                                    <input type="text" name="license_number" class="form-control" value="<%= (doctor && doctor.license_number) || '' %>" required>
                                </div>
                            </div>

                            <div class="form-row">
                                <div class="form-group">
                                    <label class="form-label">Years of Experience</label>
                                    <input type="number" name="experience_years" class="form-control" value="<%= (doctor && doctor.experience_years) || '' %>" min="0">
                                </div>
                                <div class="form-group">
                                    <label class="form-label">Consultation Fee (₹)</label>
                                    <input type="number" name="consultation_fee" class="form-control" value="<%= (doctor && doctor.consultation_fee) || '' %>" min="0" step="0.01">
                                </div>
                            </div>

                            <div class="form-group">
                                <label class="form-label">Hospital Affiliation</label>
                                <input type="text" name="hospital_affiliation" class="form-control" value="<%= (doctor && doctor.hospital_affiliation) || '' %>">
                            </div>

                            <div class="form-group">
                                <label class="form-label">Phone Number</label>
                                <input type="tel" name="phone" class="form-control" value="<%= (doctor && doctor.phone) || '' %>">
                            </div>

                            <button type="submit" class="btn btn-primary">Update Profile</button>
                        </form>
                    </div>
                </div>

                <!-- <div class="availability-section" style="margin-top: 40px;">
                    <div class="card">
                        <h2>Availability Schedule</h2>
                        <p>Set your weekly availability for appointments</p>
                        <div id="availability-schedule">
                             Availability schedule would be loaded here -->
                        <!-- </div>
                        <button class="btn btn-outline" onclick="editAvailability()">Edit Availability</button>
                    </div>
                </div> --> 

               <!--  <div class="security-section" style="margin-top: 40px;">
                    <div class="card">
                        <h2>Security</h2>
                        <button class="btn btn-outline" onclick="changePassword()">Change Password</button>
                    </div>
                </div> -->
            </main>
        </div>
    </div>

    <script>
        function changePassword() {
            const newPassword = prompt('Enter new password:');
            if (newPassword && newPassword.length >= 6) {
                alert('Password change request sent. You will receive an email with instructions.');
            } else if (newPassword) {
                alert('Password must be at least 6 characters long.');
            }
        }

        function editAvailability() {
            alert('Availability editing feature would open here.');
        }

        document.getElementById('profile-form').addEventListener('submit', async function(e) {
            e.preventDefault();
            const form = e.target;
            const data = {
                full_name: form.full_name.value,
                specialization: form.specialization.value,
                license_number: form.license_number.value,
                experience_years: form.experience_years.value,
                consultation_fee: form.consultation_fee.value,
                hospital_affiliation: form.hospital_affiliation.value,
                phone: form.phone.value
            };

            try {
                const res = await fetch('/doctor/profile', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(data)
                });
                const json = await res.json();
                if (res.ok && json.success) {
                    alert('Profile updated successfully!');
                    location.reload();
                } else {
                    alert('Failed to update profile: ' + (json.error || json.message || 'Unknown error'));
                }
            } catch (err) {
                console.error('Profile update error:', err);
                alert('Failed to update profile. Check console for details.');
            }
        });
    </script>
</body>
</html>